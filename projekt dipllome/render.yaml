services:
  - type: web
    name: sentiment-analysis
    env: python
    root_dir: projekt\ dipllome/render_project
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn Source.app:app
